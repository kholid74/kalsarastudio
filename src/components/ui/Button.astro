---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
  class?: string;
  target?: string;
  rel?: string;
  'data-wa-location'?: string;
}

const {
  href,
  variant = 'primary',
  class: className = '',
  target,
  rel,
  'data-wa-location': waLocation,
} = Astro.props;

const base    = 'inline-flex items-center justify-center gap-2 px-6 py-3 rounded-full font-body font-semibold text-sm transition-all duration-150 focus-visible:outline-2 focus-visible:outline-offset-2';
const primary = 'bg-blue text-white hover:bg-blue/90 focus-visible:outline-blue';
const secondary = 'border border-white/20 text-white/50 hover:border-white/40 hover:text-white/80 focus-visible:outline-white';

const classes = [base, variant === 'primary' ? primary : secondary, className].join(' ');
---

<a href={href} class={classes} target={target} rel={rel} data-wa-location={waLocation}>
  <slot />
</a>
