---
interface Props {
  dark?: boolean;
  class?: string;
}
const { dark = false, class: className = '' } = Astro.props;
const color = dark ? 'text-cyan' : 'text-blue';
---

<p class={`font-body font-semibold text-xs tracking-widest uppercase ${color} ${className}`}>
  <slot />
</p>
